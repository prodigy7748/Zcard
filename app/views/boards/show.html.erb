<h1><%= @board.title %></h1>

<% if user_signed_in? %>
  <%= link_to "新增文章", new_board_post_path(@board) %>
<% end %>
<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.title, post %></li>
  <% end %>
  <p>文章數量：<%= @posts.count %></p>
</ul>

